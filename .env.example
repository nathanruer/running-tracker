# ==============================================================================
# Running Tracker - Configuration Environnement
# ==============================================================================
# Copiez ce fichier en `.env` et remplissez avec vos vraies valeurs
# IMPORTANT: Ne committez JAMAIS le fichier .env (il contient des secrets)
# ==============================================================================

# ------------------------------------------------------------------------------
# Base de données PostgreSQL
# ------------------------------------------------------------------------------
# Format: postgresql://username:password@host:port/database_name
# Exemple Supabase: postgresql://postgres.xxx:[PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
DATABASE_URL="postgresql://user:password@localhost:5432/running_tracker"

# ------------------------------------------------------------------------------
# Sécurité - JWT (JSON Web Token)
# ------------------------------------------------------------------------------
# Secret pour signer les tokens de session utilisateur
# IMPORTANT: Utilisez une chaîne aléatoire longue et complexe en production
# Générer: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-change-me-in-production"

# ------------------------------------------------------------------------------
# Strava OAuth 2.0 - Authentification
# ------------------------------------------------------------------------------
# Pour obtenir ces clés:
# 1. Créez une app sur https://www.strava.com/settings/api
# 2. Notez votre Client ID et Client Secret
# 3. Configurez "Authorization Callback Domain" à "localhost" (dev) ou votre domaine (prod)

# Votre Client ID Strava (nombre public)
STRAVA_CLIENT_ID="123456"

# Votre Client Secret Strava (GARDEZ SECRET!)
STRAVA_CLIENT_SECRET="your_strava_client_secret_here"

# URL de redirection OAuth après autorisation
# Développement local:
NEXT_PUBLIC_STRAVA_REDIRECT_URI="http://localhost:3000/api/auth/strava/callback"

# Production (décommentez et adaptez):
# NEXT_PUBLIC_STRAVA_REDIRECT_URI="https://votredomaine.com/api/auth/strava/callback"

# ------------------------------------------------------------------------------
# Logging (Optionnel)
# ------------------------------------------------------------------------------
# Niveau de log: trace | debug | info | warn | error | fatal
# Par défaut: debug (dev) / info (prod)
# LOG_LEVEL="debug"

# ------------------------------------------------------------------------------
# Environnement Next.js
# ------------------------------------------------------------------------------
# Next.js détecte automatiquement, mais vous pouvez forcer:
# NODE_ENV="development"  # ou "production"
